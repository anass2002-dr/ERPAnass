export interface PurchaseDetails {
  idPurchaseDetails?: Number,
  idArticle?: Number,
  quantity?: Number,
  totalPrice?: Number,
  taxAmount?: Number,
  quality?: string,
  isActive?: boolean,
  idPurchase?: Number,
}
